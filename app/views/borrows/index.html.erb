<%= render partial: 'shared/header', locals: { url: new_borrow_path, title: 'Borrows' } %>


<div class="container text-center">
  <div class="row align-items-center">
    <table class="table table-hover table-striped">
      <thead>
      <tr>
        <th>Book</th>
        <th>User</th>
        <th>Status</th>
        <th>Borrowed at:</th>
        <th>End date at:</th>
        <th></th>
      </tr>
      </thead>
      <tbody>
      <%= render @borrows %>
      </tbody>
    </table>

    <%== pagy_bootstrap_nav(@pagy) %>
  </div>
</div>
