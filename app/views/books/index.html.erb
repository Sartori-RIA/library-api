<%= render partial: 'shared/header', locals: { url: new_book_path, title: 'Books' } %>

<div class="container text-center">
  <div class="row align-items-center">
    <%= render partial: 'shared/searchbar', locals: { url: books_url } %>

    <%= render @books %>

    <%== pagy_bootstrap_nav(@pagy) %>
  </div>
</div>
